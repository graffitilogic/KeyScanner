inputs:

uint64_t SAMPLE_POOL_SIZE = 1024 * 8196;
uint64_t NET_SIZE = SAMPLE_POOL_SIZE;


notes:

each run returning two thread matches leads me to believe there is a bug duplicating the starting keys.
